# Copy to .env.local for local dev, or add in Vercel → Project Settings → Environment Variables.
# Never commit real secrets.

# Required — Gemini answers Agent10 chat questions
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-lite-latest

# Optional — incident orchestrator (logs/Jira evidence). Not used in answers right now;
# kept ready for when you re-enable it in /api/copilot/ask.
ORCHESTRATOR_API_URL=https://incident-orchestrator-648336338393.us-central1.run.app/api/v1/orchestrate
ORCHESTRATOR_APPLICATION_ID=abcd_123
